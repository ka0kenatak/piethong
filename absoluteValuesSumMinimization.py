def absoluteValuesSumMinimization(a):

    for i in range(len(a)):
        abs(a[0] - x) + abs(a[1] - x) + ... + abs(a[a.length - 1] - x)
        #abs(a[0] - x) + abs(a[1] - x) + ... + abs(a[a.length - 1] - x)


#Declarations


#Function Calls
